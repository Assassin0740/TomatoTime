# 07 · 变更管理记录

> **文档性质**：按 PMP 整合管理中的**变更控制流程**维护的变更请求（CR）台账。
> **原则**：任何范围 / 架构级改动先登记 CR → 影响分析 → 确认 → 实施 → 验证 → 更新文档。

**变更类型**：功能新增 ｜ 缺陷修复 ｜ 优化 ｜ 文档 ｜ 重构
**状态标记**：✅ 已完成 ｜ 🔄 进行中 ｜ ❌ 已拒绝 ｜ 📋 待实施

---

## 变更台账总览

| CR 编号 | 日期 | 类型 | 摘要 | 状态 |
|---|---|---|---|---|
| CR-001 | 2026-09-11 | 优化 | 主界面低频控件折叠收纳 | ✅ 已完成 |
| CR-002 | 2026-09-11 | 功能新增 | 悬浮窗显示任务图片 + 三个内容开关 | ✅ 已完成 |
| CR-003 | 2026-09-11 | 缺陷修复 | 修复 4 项确定性缺陷 | ✅ 已完成 |
| CR-004 | 2026-09-11 | 文档 | 建立项目文档库 | ✅ 已完成 |
| CR-005 | 2026-09-11 | 重构 | 补齐版本控制（`renderer.js` / `style.css` / `index.html`） | ✅ 已完成 |
| CR-006 | 2026-09-11 | 重构 | 消除双 HTML 漂移隐患 | 📋 待实施 |

---

## CR-001 · 主界面低频控件折叠收纳

| 项 | 内容 |
|---|---|
| **提出日期** | 2026-09-11 |
| **提出人** | 项目所有者 |
| **类型** | 优化（界面 / 可用性） |
| **优先级** | 中 |
| **状态** | ✅ 已完成 |

### 变更理由

主界面控件堆叠过多：4 层筛选/排序区（排序 3 按钮 + 筛选下拉、视图切换 2 按钮、日期范围 3 按钮 + 2 日期框、类型筛选 3 按钮），加上输入区的优先级 3 项 / 类型 2 项 / 操作按钮 4 个。首屏信息密度过高，核心操作（输入任务）被淹没。

### 变更内容

把 6 组低频控件收进一个新的折叠面板 `toolPanel`，**默认收起**：

| 收纳控件 | 原位置 |
|---|---|
| 排序（时间 / 紧急 / 未完成） | 列表上方 |
| 状态筛选下拉 | 列表上方 |
| 视图切换（卡片 / 表格） | 输入区下方 |
| 任务类型筛选（全部 / 短期 / 长期） | 输入区下方 |
| 日期范围筛选（本周 / 全部时间 / 自定义） | 输入区下方 |
| 批量操作（清空 / 归档已完成 / 导出） | 输入区按钮行 |

**保留在首屏**：计时器、任务输入框、优先级、类型、「添加」按钮。

附加设计：
- 折叠头右侧显示状态摘要（如「卡片 · 本周」），收起状态也能看到当前筛选条件
- 展开/收起状态与筛选状态均持久化

### 影响分析

| 维度 | 影响 |
|---|---|
| 涉及文件 | `计时器.html`（+ `index.html` 同步）、`style.css`、`renderer.js` |
| 兼容性 | 所有元素 `id` 保持不变，不破坏 `renderer.js` 的事件绑定 |
| 交互逻辑 | 筛选下拉菜单从"绝对定位下拉"改为在折叠面板内正常布局（规避 `.collapsible-content` 的 `max-height` + `overflow:hidden` 裁剪问题） |
| 数据 | 无数据格式变更，无迁移需求 |
| 风险 | 元素 ID 缺失会导致绑定失败 → 通过引用完整性校验规避 |

### 验证结果

- ✅ 语法检查通过
- ✅ 元素引用完整性校验：HTML 中 id 与 JS 引用全部匹配
- ✅ 运行时探针确认：`toolPanel`、`toolPanelSummary`、各按钮与函数均就位；摘要显示"卡片 · 本周"
- ✅ 启动冒烟测试通过

### 文档更新

需求 → `03` FR-3.6 ｜ 架构 → `02` §2.2 ｜ 进度 → `05` M7

---

## CR-002 · 悬浮窗显示任务图片 + 内容开关

| 项 | 内容 |
|---|---|
| **提出日期** | 2026-09-11 |
| **提出人** | 项目所有者 |
| **类型** | 功能新增 |
| **优先级** | 中 |
| **状态** | ✅ 已完成 |

### 变更理由

任务内容本身是 HTML，可包含图片，但悬浮窗用 `stripHtml()` 把内容强制转为纯文本，图片完全无法显示。用户希望悬浮窗能展示任务中的图片，并提供开关控制。

### 变更内容

| 功能 | 说明 |
|---|---|
| 显示任务图片 | 从当前任务 `content` 的 HTML 中提取 `<img>`，在任务文字下方渲染为缩略图 |
| 数量上限 | 超出上限的部分显示 `+N` 角标 |
| 点击放大 | 点击图片在悬浮窗内临时放大，再点一次还原 |
| 开关 1：显示任务 | 关闭后任务区与分隔线一起隐藏，悬浮窗只剩时钟 |
| 开关 2：显示任务图片 | 关闭后只显示文字 |
| 设置项 3：最多显示图片 | 1–6 张，默认 3 |
| 自动撑高 | 开启图片且窗口高度 <160px 时自动调整到 170px，保证图片可见 |

设置入口：主界面 **⚙ → 悬浮窗设置**。

### 影响分析

| 维度 | 影响 |
|---|---|
| 涉及文件 | `float.html`、`计时器.html`（+ `index.html` 同步）、`renderer.js`、`main.js`、`style.css` |
| 新增 localStorage 键 | `floatShowTask`、`floatShowImages`、`floatImageLimit` |
| 新增 IPC 频道 | `float-content-settings`（主 → 悬浮窗）、`update-float-content-settings`（渲染 → 主） |
| 同步方式 | 沿用既有"设置存主窗口 localStorage → 主进程 `executeJavaScript` 读取 → 推送悬浮窗"模式 |
| 窗口约束 | 未修改 150–700 × 50–250 的范围限制；新增 `ensureFloatWindowFitsImages()` 做高度兜底 |
| 风险 | 图片提取需正确处理 `file://` 路径与相对路径两种 src |

### 验证结果

- ✅ `float.html` 内联脚本语法检查通过
- ✅ 悬浮窗独立运行时测试：图片成功真实加载（实测渲染宽度 1052px）
- ✅ 开关逻辑验证：关闭任务区后任务区正确隐藏
- ✅ 主界面初始化探针确认三个设置控件全部就位

### 文档更新

需求 → `03` FR-5.11 / 5.12 / 5.13 / 5.14 / 5.15 ｜ 架构 → `02` §3.2 ｜ 进度 → `05` M7

---

## CR-003 · 修复 4 项确定性缺陷

| 项 | 内容 |
|---|---|
| **提出日期** | 2026-09-11 |
| **提出人** | 项目所有者（授权"一起修"） |
| **类型** | 缺陷修复 |
| **优先级** | 高 |
| **状态** | ✅ 已完成 |

### 缺陷清单与修复方案

| 编号 | 缺陷 | 严重度 | 根因 | 修复方案 |
|---|---|---|---|---|
| **BUG-01** | 悬浮窗编辑任务会**永久丢失任务图片** | 🔴 严重（数据丢失） | 悬浮窗把 `content`（HTML）当纯文本处理，编辑后整体覆盖回写 | 编辑时仅替换文本，保留原有 `<img>` 标签 |
| **BUG-02** | 悬浮窗尺寸超过 500×200 后位置与尺寸不再保存 | 🟡 中 | 关闭时的校验范围（≤500×200）与窗口允许范围（≤700×250）不一致 | 统一为宽 150–700、高 50–250 |
| **BUG-03** | 切换工作/休息模式后托盘菜单状态不刷新 | 🟢 轻微 | `toggle-mode` 处理后未刷新托盘菜单 | 切换后调用 `updateTrayMenu()` |
| **BUG-04** | 悬浮窗右键菜单可能弹出两次 | 🟢 轻微 | 两个触发源（`context-menu` 事件 + `show-float-menu` IPC）同时生效 | 增加去重判断 |

### 影响分析

| 维度 | 影响 |
|---|---|
| 涉及文件 | `float.html`（BUG-01）、`main.js`（BUG-02 / 03 / 04） |
| 数据 | 无格式变更；已有任务的图片本就以 HTML 形式存储，修复后不再被破坏 |
| 兼容性 | 不改变现有操作习惯 |
| 回归风险 | BUG-01 的修复涉及悬浮窗编辑逻辑，需重点验证"有图任务编辑后图片仍在" |

### 验证结果

- ✅ 语法检查通过（`main.js` / `renderer.js` / `float.html` 内联脚本）
- ✅ 悬浮窗专项测试：含图任务编辑后图片保留，断言通过
- ✅ 启动冒烟测试通过
- ✅ 测试用临时文件全部清理

### 文档更新

需求 → `03` FR-8.1 / 8.2 ｜ 问题 → `06` I-08 / I-09 / I-10 / I-11 ｜ 进度 → `05` M7

---

## CR-004 · 建立项目文档库

| 项 | 内容 |
|---|---|
| **提出日期** | 2026-09-11 |
| **提出人** | 项目所有者 |
| **类型** | 文档 |
| **状态** | ✅ 已完成 |

### 变更理由

项目缺少系统化文档：新接手者无法快速理解程序如何运行，也缺少需求、架构、进度、风险、变更的可追溯记录。

### 变更内容

按 PMP 经典结构建立 `项目文档/` 目录，共 10 个文件：

| 文件 | 内容 |
|---|---|
| `README.md` | 文档导航与项目速览 |
| `01-系统运行原理.md` | 程序如何运行（启动/计时/IPC/数据/关闭） |
| `02-系统架构设计.md` | 架构、模块、接口契约、技术选型、技术债务 |
| `03-需求规格说明书.md` | 功能需求、用例、非功能需求、追踪矩阵 |
| `04-项目管理计划.md` | 章程、范围、WBS、干系人、沟通、质量、成本、采购、资源、整合 |
| `05-进度与里程碑.md` | 里程碑台账、版本演进、状态看板、Backlog |
| `06-风险与问题登记册.md` | 风险与问题登记、等级、应对 |
| `07-变更管理记录.md` | 本文件 |
| `08-测试与质量验证记录.md` | 测试策略、用例、执行记录 |
| `09-后续改进路线图.md` | 按优先级排序的改进项 |

### 影响分析

纯文档新增，**不影响任何代码行为**。

### 验证结果

- ✅ 所有描述均以实际代码为依据（逐文件核对 IPC 频道、localStorage 键、函数名、行数、Git 历史）
- ✅ 文档间交叉引用链接完整

---

## CR-005 · 补齐版本控制（待实施）

| 项 | 内容 |
|---|---|
| **类型** | 重构 / 风险控制 |
| **优先级** | **P0** |
| **状态** | ✅ 已完成（2026-09-11） |

### 变更理由

`renderer.js`（3,569 行）、`style.css`（1,780 行）、`index.html` 三个文件当前处于 Git **未跟踪**状态。这意味着占项目绝大部分的核心逻辑**没有任何版本保护**——误删、误改或磁盘损坏都无法恢复。

### 变更内容

```bash
git add renderer.js style.css index.html
git commit -m "Add untracked core source files to version control"
```

同时检查 `.agents/` 目录是否需要纳入 `.gitignore`。

### 影响分析

| 维度 | 影响 |
|---|---|
| 涉及文件 | Git 索引（无代码改动） |
| 风险 | 无。仅新增跟踪，不改内容 |
| 收益 | 核心代码获得版本保护与变更历史 |

### 实施结果

实际拆分为 3 个提交执行，工作区已干净：

| 提交 | 说明 | 规模 |
|---|---|---|
| `50e51fc` | 将未被跟踪的核心源文件纳入版本控制 | 3 files, +5,713 行 |
| `574ef85` | 主界面控件折叠 + 悬浮窗任务图片与开关 + 修复 4 项缺陷 | 8 files, +1,069 / −4,244 |
| `82b8d73` | 新增 PMP 项目文档库 | 10 files, +2,381 行 |

附加处理：
- `.gitignore` 新增 `.workbuddy/`（本地工作记忆，非交付物）
- `.agents/AGENTS.md` 纳入版本控制（项目规则文件）

### 实施中的发现

提交时发现 `计时器.html` 的差异为 **−4,331 行**，说明仓库中原本存着的是"内联了全部逻辑的大文件"，后来的拆分重构（拆为 `计时器.html` + `renderer.js` + `style.css`）**从未被提交过**。这正好解释了为什么这三个文件一直是未跟踪状态。

### 待确认事项（已处理）

- ~~需确认 `index.html` 是否保留~~ → 暂按现状提交以先取得版本保护，其去留由 CR-006 决定

---

## CR-006 · 消除双 HTML 漂移隐患（待实施）

| 项 | 内容 |
|---|---|
| **类型** | 重构 |
| **优先级** | **P0** |
| **状态** | 📋 待实施 |

### 变更理由

`main.js` 实际加载的是 `计时器.html`，但仓库中同时存在内容基本一致的 `index.html`。两者必须手工同步，极易漂移——一旦有人修改了 `index.html`，会得到"改了没生效"的困惑。

### 候选方案

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A（推荐）** | 删除 `index.html`，在 README 与文档中明确"主界面只有 `计时器.html`" | 彻底消除隐患，最简单 | 需确认无外部引用 |
| B | 保留 `index.html` 作为软链接 / 构建时拷贝 | 兼容按惯例找 `index.html` 的工具 | Windows 下软链需权限 |
| C | 增加构建前校验脚本，检测两份文件是否一致 | 保留双份 | 未从根本解决，仍需同步 |

### 影响分析

| 维度 | 影响 |
|---|---|
| 涉及文件 | `index.html`（删除或改造）、`main.js`（若改加载路径） |
| 风险 | 若有代码/脚本依赖 `index.html` 会受影响 → 需先全仓搜索引用 |

### 待确认事项

- `main.js` 是否需要在某些场景下加载 `index.html`
- 打包配置中是否引用了 `index.html`

---

## 变更控制流程回顾

本次全部变更（CR-001 ~ CR-004）均遵循了以下流程：

```
① 提出需求
② 影响分析（涉及文件 / 数据风险 / 兼容性）
③ 方向确认（涉及取舍时提供选项，例如"折叠收纳 vs 精简合并"）
④ 实施
⑤ 验证（语法检查 → 引用校验 → 启动冒烟 → 专项测试）
⑥ 更新文档
⑦ 关闭 CR
```

**流程执行中的一次关键决策**：在实施前，针对"主界面如何优化""悬浮窗显示哪些图片""是否一并修 bug"三个方向提供了选项让所有者确认，避免了返工。此做法已沉淀为沟通原则（见 `04` §5）。
