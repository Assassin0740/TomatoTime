# 06 · 风险与问题登记册

> **文档性质**：按 PMP 风险管理流程维护的**风险登记册（Risks）**与**问题登记册（Issues）**。
> **区别**：**风险** = 尚未发生、可能带来负面影响的事件；**问题** = 已经发生、需要处理的事件。
> 更新日期：2026-09-11

**等级 = 概率 × 影响**：🔴 高 ｜ 🟡 中 ｜ 🟢 低

---

## 一、风险登记册（尚未发生）

| 编号 | 风险描述 | 类别 | 概率 | 影响 | 等级 | 应对策略 | 应对措施 |
|---|---|---|---|---|---|---|---|
| ~~**R-01**~~ | ~~`计时器.html` 与 `index.html` 内容漂移，导致"改了没生效"或两处行为不一致~~ | 技术 | 高 | 高 | ✅ **已关闭** | 规避 | 2026-09-11 删除 `index.html`，主界面仅保留 `计时器.html` 单一来源（CR-006） |
| **R-02** | 渲染进程具备完整 Node 权限（`nodeIntegration: true` + `contextIsolation: false` + `webSecurity: false`），一旦加载外部/不可信内容即可读写本地文件 | 安全 | 中 | 高 | 🔴 | 减轻 | 迁移到 `contextIsolation: true` + `preload` + `contextBridge`，按需暴露 API |
| **R-03** | `renderer.js` 单文件 3,569 行，无模块边界，修改易引发连锁回归 | 技术 | 高 | 中 | 🔴 | 减轻 | 按功能拆分为多个模块（可用原生 ES Module，无需构建工具） |
| **R-04** | 无自动化测试，仅靠手工冒烟，回归缺陷难以发现 | 质量 | 高 | 中 | 🔴 | 减轻 | 优先为计时器状态机、任务 CRUD、图片保留三块补测试 |
| **R-05** | localStorage 有容量上限（约 5–10MB），任务量极大或产生大量评论时可能写入失败 | 技术 | 低 | 高 | 🟡 | 减轻 | ① 已用内容寻址把图片移出 localStorage；② 增加写入失败检测与提示；③ 定期归档 |
| **R-06** | Electron 28 已进入维护后期，缺失安全补丁 | 依赖 | 中 | 中 | 🟡 | 接受 / 减轻 | 评估升级到受支持版本，回归测试托盘、悬浮窗透明等平台相关功能 |
| **R-07** | 安装包含完整 Electron 运行时（100MB+），分发与更新成本高 | 成本 | 中 | 低 | 🟢 | 接受 | 个人使用场景可接受 |
| **R-08** | 无 GPU 环境下启动会触发 GPU 进程 FATAL（开发/自动化场景） | 环境 | 中 | 低 | 🟢 | 规避 | 使用 `--disable-gpu --disable-gpu-compositing --disable-software-rasterizer --in-process-gpu` |
| ~~**R-09**~~ | ~~版本号不一致：`package.json` 为 `1.0.5`，应用"关于"对话框显示 `v1.0.0`~~ | 一致性 | 高 | 低 | ✅ **已关闭** | 规避 | 2026-09-11 改为 `app.getVersion()` 动态读取，单一来源 |
| **R-10** | 设置同步依赖"主进程用 `executeJavaScript` 读主窗口 localStorage 再推送"，异步时序脆弱 | 技术 | 中 | 中 | 🟡 | 减轻 | 设置迁到主进程统一持久化（如 `settings.json`），通过统一 IPC 读写 |
| **R-11** | 主窗口与悬浮窗各自实现任务内容解析（图片提取、HTML 处理），格式一旦变更容易只改一处 | 技术 | 中 | 中 | 🟡 | 减轻 | 抽取共享工具模块供两端引入 |
| **R-12** | 用户数据（localStorage + 图片文件）无自动备份，磁盘故障或误操作会导致任务全部丢失 | 数据 | 低 | 高 | 🟡 | 减轻 | ① 已有 JSON 导出功能（FR-7.6）；② 建议增加定期自动备份或导出提醒 |
| **R-13** | `dist/` 打包产物与当前源码不同步，用户可能一直在用旧版本 | 运营 | 中 | 中 | 🟡 | 规避 | 每次改动后明确提示"需重新 `npm run build`"；或建立发布检查清单 |
| ~~**R-14**~~ | ~~悬浮窗图片区与独立配图功能（右键菜单"选择图片"）并存，概念重叠易混淆~~ | 体验 | 中 | 低 | ✅ **已缓解** | 规避 | 2026-09-11 右键菜单改名为"设置悬浮窗配图… / 清除悬浮窗配图"，与"任务图片"明确区分；若日后仍嫌冗余可整项移除 |

---

## 二、问题登记册（已发生）

### 2.1 未解决的问题

| 编号 | 问题描述 | 严重度 | 发现时间 | 状态 | 建议处理 |
|---|---|---|---|---|---|
| **I-02** | 文字阴影实现方式："阴影大小"作用在时间数字上使用的是 `box-shadow`，形成矩形光晕块，而非真正的文字阴影 | 🟢 轻微 | 2026-09-11 | ❌ 未处理 | 需确认是否为有意取舍（避免与"发光"效果冲突）；确认后可改为 `text-shadow` |
| **I-03** | 悬浮窗右键菜单仍保留"选择图片 / 清除图片"，与新增的"任务图片"功能概念重叠 | 🟢 轻微 | 2026-09-11 | ❌ 待决策 | 若任务图片已满足需求，可移除该菜单项 |
| **I-04** | 死代码：`renderer.js` 中 `floatWin` 变量恒为 `null`，`updateTimeDisplay()` 中操作它的分支永不执行 | 🟢 轻微 | 2026-09-11 | ❌ 未处理 | 清理该变量与分支 |
| **I-05** | 无效配置：`webPreferences.enableRemoteModule: true` —— Electron 14+ 已移除 remote 模块，该配置不生效 | 🟢 轻微 | 2026-09-11 | ❌ 未处理 | 移除该配置项 |
| **I-06** | `package.json` 的 `description` / `author` 为占位值（`专注计时器应用` / `User`），应用"关于"对话框版本号与 `package.json` 不一致 | 🟢 轻微 | 2026-09-11 | ❌ 未处理 | 补齐元信息，统一版本号 |
| **I-07** | 仓库中存在 `.agents/` 目录且未被 `.gitignore` 覆盖，可能混入无关文件 | 🟢 轻微 | 2026-09-11 | ❌ 未处理 | 确认用途后纳入忽略或删除 |

### 2.2 已解决的问题

| 编号 | 问题描述 | 严重度 | 发生时间 | 状态 | 解决方案 |
|---|---|---|---|---|---|
| **I-01** | **核心源文件未纳入版本控制** —— `renderer.js`（3,569 行）、`style.css`（1,780 行）、`index.html` 处于 Git 未跟踪状态，核心逻辑无任何版本保护 | 🔴 **严重** | 2026-09-11 前 | ✅ 已修复 | 拆 3 个提交补齐（`50e51fc` / `574ef85` / `82b8d73`）；`.workbuddy/` 加入 `.gitignore`。详见 `07` CR-005 |
| **I-08** | **悬浮窗编辑任务会永久丢失任务图片** —— 悬浮窗把 `content`（HTML）当作纯文本处理，编辑后整体覆盖回写，任务内的 `<img>` 全部消失 | 🔴 严重（数据丢失） | 2026-09-11 前 | ✅ 已修复 | 编辑时仅替换文本节点，保留原有 `<img>` 标签；并在文档中把"`content` 是 HTML"列为头号约定 |
| **I-09** | 悬浮窗尺寸超过 500×200 后，关闭时位置与尺寸不再保存（校验范围与窗口允许范围不一致） | 🟡 中 | 2026-09-11 | ✅ 已修复 | 统一三处校验为宽 150–700、高 50–250 |
| **I-10** | 切换工作/休息模式后，托盘菜单状态文字不刷新 | 🟢 轻微 | 2026-09-11 | ✅ 已修复 | `toggle-mode` 后调用 `updateTrayMenu()` |
| **I-11** | 悬浮窗右键菜单可能弹出两次（两个触发源同时生效） | 🟢 轻微 | 2026-09-11 | ✅ 已修复 | 增加去重判断 |
| **I-12** | 启动即报 `ReferenceError` / TDZ 错误（一天内 4 次） | 🔴 严重 | 2026-06-29 | ✅ 已修复 | 提交 `cab85f4`、`942f869`、`a048711`；并建立"改动后必做启动冒烟测试"的质量约定 |
| **I-13** | 滚动触发渲染 → 渲染再触发滚动的死循环 | 🟡 中 | 2026-06-29 | ✅ 已修复 | 提交 `f7a27c7` |
| **I-14** | 图片懒加载使用未编码的 SVG data URI 导致加载异常 | 🟡 中 | 2026-06-29 | ✅ 已修复 | 改为 base64 编码（提交 `f7a27c7`） |
| **I-15** | 全时间筛选失效、活跃/归档卡片未能正确合并 | 🟡 中 | 2026-06-29 | ✅ 已修复 | 提交 `b285bc3` |
| **I-02** | 文字阴影用 `box-shadow` 实现，在时间数字周围形成矩形光晕块，而非真正的文字阴影 | 🟢 轻微 | 2026-09-11 | ✅ 已修复 | 改用 CSS 变量 `--text-shadow` + `text-shadow`，并与"发光"效果**叠加**而非互相覆盖（`float.html`） |
| **I-03** | 悬浮窗右键菜单"选择图片 / 清除图片"与新增"任务图片"概念重叠 | 🟢 轻微 | 2026-09-11 | ✅ 已缓解 | 改名"设置悬浮窗配图… / 清除悬浮窗配图"，明确其独立配图定位 |
| **I-04** | 死代码：`renderer.js` 中 `floatWin` 恒为 `null`，相关分支永不执行 | 🟢 轻微 | 2026-09-11 | ✅ 已修复 | 删除变量与永不执行的分支 |
| **I-05** | 无效配置 `webPreferences.enableRemoteModule: true`（Electron 14+ 已移除 remote 模块） | 🟢 轻微 | 2026-09-11 | ✅ 已修复 | 两处配置均已移除 |
| **I-17** | **计时到点后只切换阶段、不自动开始，需手动点「开始」**，与"专注—休息"连续循环的使用直觉不符 | 🟡 中 | 2026-09-11 | ✅ 已修复 | 新增设置项「自动开始下一阶段」（默认关闭）：开启后主进程在归零分支保留 interval 继续跑下一阶段，且不重置 `lastTime` 以携带不足 1 秒的余量、避免累积漂移 |

---

## 三、风险态势小结

| 维度 | 当前状况 |
|---|---|
| **最高优先级敞口** | 原 I-01（核心代码无版本控制）、R-01（双 HTML 漂移）**均已闭环**。当前最高优先级转为 **R-02 渲染进程安全边界缺失** 与 **R-03 单文件过大** |
| **架构性风险** | R-03（单文件过大）、R-10（设置同步脆弱）、R-11（解析逻辑重复）—— 建议纳入下一阶段迭代。R-01 已消除 |
| **安全风险** | R-02 —— 单机本地使用场景下实际暴露面有限，但作为长期债务应逐步收敛 |
| **数据风险** | 严重的图片丢失问题（I-08）与版本保护缺失（I-01）均已闭环；R-05、R-12 属中低概率，已有部分缓解手段 |
| **一致性风险** | R-09（版本号不一致）已闭环；R-13（`dist/` 与源码不同步）仍存在，靠流程提示规避 |
| **已闭环** | 16 个问题已修复，其中 3 个严重级（I-01 版本保护缺失、I-08 数据丢失、I-12 启动崩溃）；2 项风险（R-01 / R-09）已关闭，1 项（R-14）已缓解 |

### 风险应对优先级建议

```
立即处理（成本极低、收益极高）
  └─ I-01  补齐版本控制                      ✅ 已完成（2026-09-11）

近期处理（一轮迭代内）
  ├─ R-01  消除双 HTML                       ✅ 已完成（2026-09-11）
  ├─ R-09  统一版本号                        ✅ 已完成（2026-09-11）
  ├─ I-02 ~ I-05  阴影 / 命名 / 死代码 / 无效配置   ✅ 已完成（2026-09-11）
  ├─ I-06  补齐 package.json 元信息           ← 仅剩 description / author
  └─ P2-3  数据与设置集中化（模块化与移动端的共同前置）  ← 建议提到最前

中期处理（需规划）
  ├─ R-02  安全加固
  ├─ R-03  模块化拆分
  ├─ R-04  自动化测试
  └─ R-10  设置集中化

长期/接受
  ├─ R-06  Electron 升级评估
  └─ R-07 / R-08  成本与环境问题
```

> **当前实际卡点**：`R-01` / `R-09` 关闭后，剩余所有中长线事项（模块化、自动化测试、移动端）都指向同一个前置——**`P2-3` 数据与设置集中化**。建议下一轮优先处理。
